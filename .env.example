# Copy to .env and fill in. Optional: without these, the script only prints to console.
# Create a bot via @BotFather -> /newbot, paste the HTTP API token here:
BOT_TOKEN=
# Your Telegram chat id (message the bot, then open
# https://api.telegram.org/bot<TOKEN>/getUpdates and read chat.id):
CHAT_ID=
